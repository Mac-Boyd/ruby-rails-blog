
<h1>Listing Articles<img src="https://upload.wikimedia.org/wikipedia/commons/5/51/Night-sky-milky-way-galaxy-astrophotography_-_West_Virginia_-_ForestWander.jpg"></h1>
<%= link_to 'New Article', new_article_path, class: "big button"%>
 <table>
  <tr>
    <th>Title</th>
    <th>Text</th>
    <th>Actions</th>
  </tr>
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
      <td><td><%= link_to 'Show', article_path(article), class: "small button" %></td>
        </td>
     <td><%= link_to 'Delete', article_path(article),  class: "small button", 
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </td>
    </tr>
  <% end %>
</table>
